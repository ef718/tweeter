.container-fluid.main
  .row
    .col-xs-12
      %h1 Twitter User Search

  .row
    .col-xs-12
      = form_tag("/search", method: "get") do
        = label_tag(:q, "Search for a Username:")
        = text_field_tag(:q)
        = submit_tag("Search")
